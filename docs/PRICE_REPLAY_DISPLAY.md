# 价格线与买卖点回放展示

用户授权用价格折线、买卖标记和股数替换复盘大屏主图，保留 Dynamic Replay 与既有交互。该变更仅涉及数据展示，不改变交易模型或下单逻辑。

## 页面

- `TradeComparisonPanel` 默认显示新的价格回放；经典规则轨迹与K线保留在旁边的切换按钮。
- `PriceActionReplayChart` 显示蓝色价格折线、橙色OHLCV均价代理、B/S/C/X及股数、成交量、持仓和净盈亏。支持股票、日期、研究模型、播放/暂停、倍速、进度拖动、时段与缩放。
- 原Dynamic Replay保留沙盒、播放按钮、倍速和其他页面结构，图表默认改为价格线与均价代理。K线及EMA可通过图例开启。
- 股票在两个展示区域联动。研究回放和券商归档的日期独立选择，因为保存的日期范围可能不同；不能把券商归档日期强塞给研究回放。
- 修复原 `trade_comparison` 页面重复渲染两套复盘面板的问题。

## 数据

新增只读接口：

```text
GET /api/dashboard/price_replay?ticker=SNDK&date=2026-09-15&variant=levels_error_risk
```

当前可移植数据包位于 `backend/data/price_replay/`，覆盖SNDK、TSLA、NVDA、PLTR，9月14日与9月15日，Context/支撑压力/支撑压力与误差风险三种研究模型，共24份。该接口读取已保存数据，不训练模型、不查询券商、不自动生成交易。缺失组合显示不可用。

9月15日仅截至15:50美东，仍保留未平仓库存；不能标成完整日或实际账户盈亏。单边5bps为模拟摩擦，已体现在模拟成交价，不能再次从净盈亏扣除。组合期初资金不等于某只股票独占的资金。

价格时间使用已完成5分钟K线的可用时刻，成交使用实际模拟执行时间。持仓阶梯直接来自成交事件。保存的K线收盘估值发生在同刻下一根开盘成交之前，因此前端追加同刻成交后的现金与库存估值，使回放不会出现新仓位却仍显示成交前盈亏的情况。重置/播放不显示尚未发生的成交标记。

原Dynamic Replay保留券商归档来源。其真实成交标记现在按完整时间戳定位，不再要求成交分钟恰好等于5分钟K线时间点。

## 更新研究数据包

使用导出脚本读取经过校验的研究产物，命令行参数见：

```bash
python3 scripts/export_price_replay_bundle.py --help
```

不要手动修改保存的盈亏或股数；清单哈希不匹配的产物会被拒绝展示。新报告加入页面需要更新数据包，而不是将当前两天样本假装成实时推断。

## 验证

- `PYTHONPATH=backend python3 -m pytest -q backend/tests/test_price_replay.py`：7项通过，包含24份数据的逐次现金/库存重算、未平仓、时间可用性、错误请求及哈希异常。
- `npm run build`：TypeScript和Vite构建通过。
- Chrome离线浏览器检查：四股、日期/模型切换、原报告盈亏与成交数、重置、播放/暂停/倍速、缩放、窄屏、研究和券商日期独立；无浏览器运行异常。新主图使用真实保存的研究数据，测试不连接券商。
- Dynamic Replay浏览器检查：历史价格线、可选K线、09:32:26这类非整5分钟成交标记、播放中无未来标记、图例保持以及快速换股票后的请求竞争。

截图与检查摘要：`reports/price_replay_ui_20260915/`。本次未修改 `live_runner.py` 或替换线上模型。

## 每日券商模式（2026-09-16）

主图默认 `source=broker`，未传日期时显示美东当天，每15秒只读刷新。日期覆盖券商活动以来的交易日；`source=research` 保留原研究包。账户FILL活动分页读取并按ID去重，缓存按账户隔离存放在忽略目录 `backend/.runtime_state/broker_replay`。今日使用IEX行情，历史使用SIP行情；两者覆盖不同，页面显示来源。仅展示已完成5分钟价格，成交保留实际时间。

买卖反转拆分为平仓/开仓标记；无法与当前库存核对时显示BUY/SELL且持仓与盈亏留空。日内盈亏包含相对前收的隔夜持仓市值变化，FIFO已实现盈亏另列，二者不等于账户净值变化。使用实际成交价，不重复扣模拟5bps；券商费用尚未核实。盘前盘后成交自动扩展横轴。

最新验证：4项券商核算测试及前端生产构建通过。两天SNDK真实Paper成交与TSLA X63反事实对照见 `reports/daily_broker_replay_20260916/REPORT.md`。

## 日期入口与导航修复（2026-09-17）

- `GET /api/dashboard/price_replay_dates` 返回独立于当前来源的日期目录，以及当前股票/模型真正存在的研究日期。
- 研究模拟日期下拉也保留今天、昨天和其他交易日。选择尚未生成实验的日期时，切到券商成交并显示提示；不会生成或冒充研究收益。
- 新增“今天 · 自动更新”按钮。原研究模型仍可选择，原券商成交每15秒刷新不变。
- 五个常用导航统一44px高度，缩短标签；“代理指标/L1数据状态”移入“更多工具”，保留原页面，窄屏导航横向滚动。
- 验证：11项既有回放/核算测试、日期目录检查、TypeScript/Vite构建通过；Chrome验证研究模式包含9/16和9/17，选择9/16显示券商来源提示，今天按钮回到9/17，1440px与390px导航高度一致，无JS运行异常。

本次没有修改 `live_runner.py`、模型产物或交易决策。线上9/17 09:45读取到的执行模型为 `price_volume_h1_g50`（trained_before 2026-09-12，L1未参与）。SNDK当时可核对成交为09:35买20股@1606.58、09:40卖20股@1601.41，实际成交FIFO已实现−103.40美元、额外费用未核。没有保存这两次完整历史预测输入，因此不能把后来价格走势作为已证实的模型归因。
